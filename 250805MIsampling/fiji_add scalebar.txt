

dir=getDirectory("Choose an Input Folder");
list=getFileList(dir);

for (i = 0; i < list.length; i++) {
        path = dir + list[i];

       
        open(path);
        run("Scale Bar...", "width=100 height=50 thickness=10 font=0 color=Black bold");

            // —— 覆盖保存为 TIFF ——
title = getTitle();

            saveAs("Tiff",'/Volumes/WT-WD/扫片/亦然/scalebaradded/'+title+'.tiff');
            run("Close All");
        }